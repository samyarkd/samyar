export default function Home() {
  return (
    <main className="max-w-6xl mx-auto flex min-h-screen flex-col items-center justify-between p-4 sm:p-24 sm:pt-12">
      <div className="z-10 max-w-5xl w-full items-center justify-between text-xl lg:flex">
        <p>This is by far the most hassle free and best way to implement this. It is purely CSS and very very simple to do, no extra files - nothing. Ok, it's not the best way possible, but it works very well, very reliable (never failed when testing across very old browsers) and very fast to load.

          Found it a few months ago, and used it ever since, simply copy and paste this code in to your CSS.</p>
      </div>
    </main>
  )
}
