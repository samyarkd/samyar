'use client'

import { type ReactNode } from 'react'

const Providers = ({ children }: { children: ReactNode }) => {
  return (
    <>
      {/* <StarsBackground /> */}

      {children}
    </>
  )
}

export default Providers
